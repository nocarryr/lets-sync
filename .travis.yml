language: python

python:
  - "2.7"
  - "3.4"
  - "3.5"
install:
  - pip install PyOpenSSL pytest-cov coveralls
  - pip install -e .
script:
  - py.test --cov-config .coveragerc --cov=letssync
after_success:
  - coveralls
